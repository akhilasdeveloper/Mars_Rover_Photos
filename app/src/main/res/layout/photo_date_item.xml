<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/photo_date_item_height"
    android:gravity="bottom"
    android:background="@drawable/imageview_placeholder"
    android:layout_margin="@dimen/stroke_medium"
    android:orientation="vertical">

    <TextView
        android:id="@+id/date"
        android:layout_width="match_parent"
        android:layout_height="@dimen/toolbar_top_title_text_height"
        style="@style/photo_item_date"/>

    <include layout="@layout/photo_item"
        android:id="@+id/photo_item"/>
</LinearLayout>